{#
/**
 * @file
 * Default template for a field.
 */
#}
{% set classes = [
  'companyinformation'
] %}
<div class="companyinformation__wrapper">
  {% for item in items %}
    <div{{ attributes.addClass(classes) }}>{{ item.content }}</div>
  {% endfor %}
</div>
